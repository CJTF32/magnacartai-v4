name = "magnacartai-v4"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# Set in Cloudflare Pages dashboard → Settings → Environment variables → Production:
#   OPENAI_API_KEY    — platform.openai.com        (GPT-5.4 nano)
#   ANTHROPIC_API_KEY — console.anthropic.com      (Claude Haiku 4.5)
#   XAI_API_KEY       — console.x.ai               (Grok 3 mini)
#   MISTRAL_API_KEY   — console.mistral.ai         (Mistral Large 2)
#   GEMINI_API_KEY    — aistudio.google.com        (Gemini 2.5 Flash — default judge)
